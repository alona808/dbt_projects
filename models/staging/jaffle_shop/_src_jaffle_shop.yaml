sources:
  - name: jaffle_shop
    database: dbt-tutorial  # The remote project
    schema: jaffle_shop
    tables:
      - name: customers
      - name: orders
        config:
          freshness:
            warn_after:
              count: 24
              period: hour
            error_after:
              count: 16
              period: hour

          loaded_at_field: _etl_loaded_at

